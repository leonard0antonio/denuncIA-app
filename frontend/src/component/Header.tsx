import styled from "styled-components";

const Bar = styled.header`
  width: 100%;
  padding: 16px;
  background: #111;
  color: #fff;
  font-size: 20px;
`;

export default function Header() {
  return <Bar>DenuncIA</Bar>;
}
